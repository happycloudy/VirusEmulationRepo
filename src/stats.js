const initialState = {
    virusReproduces: {
        mutation: 0,
        translocation: 0,
    },
    reproduceRatedFactor: {
        mutation: 0,
        translocation: 0,
    }
}

const stats = {...initialState}

module.exports = stats